export enum Condition {
    UNTIL_ROLL_COUNT = "Kill count",
    UNTIL_SHARD_COUNT = "Shard count",
    UNTIL_UNIQUE_COUNT = "Unique count",
    UNTIL_PET_DROP = "Pet count",
    UNTIL_SELECTED_ITEMS = "Selected items",
    UNTIL_ALL_ITEMS = "All items",
}